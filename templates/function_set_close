		if(!isset($array))
            		$array=$this;
        	$this->db->insert('stats',$array);
		return $this->db->insert_id();
		//print_r($this->db->last_query()); //per vedere la query appena eseguita
	}
